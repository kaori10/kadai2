<% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

<% if flash[:success] %>
<p><%=flash[:success] %></p>
<% end %>

<p>Title:</p>
<p><%= @book.title %></p>

<p>Body:</p>
<p><%= @book.body %></p>

<%= link_to "Edit", edit_book_path(@book.id) %> |
<%= link_to "Back", books_path %>